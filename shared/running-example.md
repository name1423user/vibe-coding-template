# 通し課題（確定仕様）

全編でこの1つを作り続ける。章ごとに別の例を出さない。

## 状態

**確定済み。**

## 満たすべき4条件

題材は以下をすべて満たすこと。1つでも欠けると第4部（安全編）と第5部（運用編）が空回りする。

1. データを保存する
2. 複数の画面がある
3. ログインがある
4. 公開できる

## 確定後に埋める項目

- **題材**：読書記録アプリ「ReadLog」

- **1文の説明**：ログインした個人が、読んだ本のタイトルと感想、読了日を記録し、一覧で振り返れるアプリ

- **やらないことリスト**：
  - 他のユーザーの記録を閲覧できるようにしない（自分の記録だけを扱う）
  - 本の情報を外部API（書籍情報サービスなど）から自動取得しない。手入力のみ
  - 感想へのコメントやSNS共有などのソーシャル機能は作らない
  - 読書の途中経過（ページ数）は記録しない。読了日とステータスだけを扱う

- **画面一覧**：
  1. ログイン画面
  2. 新規登録画面
  3. 本の一覧画面（自分が登録した本を読了日順に表示）
  4. 本の登録・編集画面
  5. 本の詳細画面（感想を表示）

- **機能一覧（最小構成）**：
  - ユーザー登録ができる
  - ログインができる
  - ログアウトができる
  - 本を登録できる（タイトル・著者・感想・読了日・ステータス）
  - 自分が登録した本の一覧を見られる
  - 本の詳細（感想）を見られる
  - 本を編集できる
  - 本を削除できる

- **データ構造**：
  - `users`：id、email、password_hash、created_at
  - `books`：id、user_id、title、author、review、finished_on、status（未読／読書中／読了）、created_at
  - `books.user_id` は `users.id` を参照する（自分の本だけが見える、の根拠になる）

- **技術スタック**：Node.js + Express + SQLite（better-sqlite3）。認証はセッションベース（express-session）、パスワードは bcrypt でハッシュ化。画面は EJS テンプレート。環境変数は dotenv で読み込む（`.env` の値は自動では読み込まれないため）

- **例示コードの言語**：JavaScript（Node.js）（手順は言語非依存だが、例は本書全体でこの1言語に固定する）

## 変更の手順

仕様を変えるときは、原稿より先にこのファイルを更新する。
更新したら、影響する章を洗い出して修正する。
